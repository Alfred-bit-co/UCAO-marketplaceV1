# AI recommendations parked for later

This folder preserves the previous DeepSeek product-description feature.

It is intentionally not imported by the Flask payment micro-service. Reintegrate it later from the Next.js/Supabase architecture, ideally as a separate API route or worker that checks Supabase Auth, the seller role, and rate limits before calling DeepSeek.
